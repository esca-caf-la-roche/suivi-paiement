// Types TypeScript pour la base de données Supabase
// Mis à jour au fur et à mesure des migrations

export interface Responsible {
  id: string        // uuid = auth.users.id
  name: string      // "Patrick C" ou "Stéphane M"
}
